require "spec_helper"

RSpec.describe S3::Http::Get do
  it "has a version number" do
    expect(S3::Http::Get::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
